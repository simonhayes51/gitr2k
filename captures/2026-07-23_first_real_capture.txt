First real runtime capture against the genuine gitr2k.exe/arena.exe clients,
via a binary-patched copy pointed at the Railway-hosted meta server
(hostname + port patched directly into the DFM resources; see git history
for the patch). Captured from the meta server's own stdout logging
(server/logger.py's per-connection RECV heuristics), copied from Railway's
log viewer.

[2026-07-23 07:34:28.384] CONNECT 100.64.0.10:10908
[2026-07-23 07:34:28.385] RECV 100.64.0.10:10908  len=24
  00000000  43 4c 41 55 54 48 20 54 65 73 74 20 6e 6f 6e 65  |CLAUTH Test none|
  00000010  20 4d 43 43 20 30 0d 0a                          | MCC 0..|
  ASCII: b'CLAUTH Test none MCC 0\r\n'
  Heuristics: {'contains_crlf': True, 'contains_lf_only': False, 'contains_cr_only': False, 'printable_ratio': 0.917, 'candidate_delimiters': [{'byte': ' ', 'count': 4}], 'notes': []}
  Source: arena.exe (GUI showed "arena name: Test", "arena password: ####")

[2026-07-23 07:35:10.471] CONNECT 100.64.0.10:37408
[2026-07-23 07:35:10.472] RECV 100.64.0.10:37408  len=27
  00000000  43 4c 41 55 54 48 20 4e 49 4c 20 4e 49 4c 20 47  |CLAUTH NIL NIL G|
  00000010  49 54 52 4e 45 57 53 20 30 0d 0a                 |ITRNEWS 0..|
  ASCII: b'CLAUTH NIL NIL GITRNEWS 0\r\n'
  Heuristics: {'contains_crlf': True, 'contains_lf_only': False, 'contains_cr_only': False, 'printable_ratio': 0.926, 'candidate_delimiters': [{'byte': ' ', 'count': 4}], 'notes': []}
  Source: gitr2k.exe, sent automatically on startup before any login step

[2026-07-23 07:35:14.637] CONNECT 100.64.0.4:42830
[2026-07-23 07:35:14.661] RECV 100.64.0.4:42830  len=29
  00000000  43 4c 41 55 54 48 20 20 4e 49 4c 20 4d 45 54 41  |CLAUTH  NIL META|
  00000010  41 52 45 4e 41 4c 49 53 54 20 30 0d 0a           |ARENALIST 0..|
  ASCII: b'CLAUTH  NIL METAARENALIST 0\r\n'
  Heuristics: {'contains_crlf': True, 'contains_lf_only': False, 'contains_cr_only': False, 'printable_ratio': 0.931, 'candidate_delimiters': [{'byte': ' ', 'count': 4}], 'notes': []}
  Source: gitr2k.exe, sent when the "Select Arena" dialog opened (shows
          "retrieving country list" - this request never got a response,
          so the dialog just spins forever)

--- Second test session, after fixing the CLAUTH dispatch bug ---
Server now recognizes and responds to commands. arena.exe registered
"Test" via MCC as before (no response, as designed). gitr2k.exe's
sequence on ONE connection:

  > CLAUTH  NIL METAARENALIST 0\r\n
  < METAARENALIST Test 0 20\r\nENDARENALIST\r\n   (our experimental guess)
  > RETRCOUNTRIES\r\n   (bare, no CLAUTH envelope - sent on the same
                         already-open connection as the line above)
  < (no response sent yet at the time of this capture)
  (connection then timed out and disconnected ~10s later)

RETRCOUNTRIES is a brand new token, not previously known. Investigating
it led to finding COUNTRY, ARENA, and ENDCOUNTRYLIST as compiled string
constants in gitr2k.exe (same data table as METAARENALIST/ENDARENALIST),
which we didn't know to look for before this. See docs/protocol.md
section 3 and commands/retrcountries.py for the follow-up experimental
response now deployed (COUNTRY/ARENA/ENDCOUNTRYLIST shape), which is
untested as of this file being written - awaiting the next real client
reaction.

--- Third test session: experiment #2 (COUNTRY/ARENA/ENDCOUNTRYLIST) result ---
Server correctly responded to RETRCOUNTRIES this time (registry duplicate
bug also fixed - exactly one "Test" arena now, not two):

  < COUNTRY International\r\nARENA Test 0 20\r\nENDCOUNTRYLIST\r\n

Client's "Select Arena" dialog reacted with a real, generic Delphi error
string found in the binary: "an error has occured." (status bar text),
instead of populating or continuing to wait. This is meaningful negative
feedback - the client processed the response far enough to hit a
validation/parsing failure, rather than ignoring it or timing out again.

Traced the error string to a small function (VA 0x54eea8 in
gitr2k.exe) that sets a status label to this text and toggles another
control. Exhaustively scanned every CALL rel32 instruction in the
entire CODE section for both this function and the METAARENALIST getter
(0x54d01c) - zero direct call sites found for either, confirming both
are invoked purely through Delphi virtual dispatch. Static disassembly
is at a hard wall here without better tooling.

Switching to isolating the variable empirically: next test sends only a
bare ENDCOUNTRYLIST\r\n (no COUNTRY/ARENA lines) to determine whether
the error comes from the per-item line format or something more basic.

--- Fourth test session: experiment #3 (bare ENDCOUNTRYLIST) result ---
Sent: ENDCOUNTRYLIST\r\n (no COUNTRY/ARENA lines at all)
Client reaction: "Select Arena" dialog showed a clean, normal prompt -
"No arenas online. Start own arena server?" (Yes/No) - NOT an error.

This isolates the problem precisely: basic RETRCOUNTRIES framing and
the ENDCOUNTRYLIST terminator are both fine on their own. Experiment #2's
"an error has occured." must have come specifically from the COUNTRY/
ARENA per-item line format.

Experiment #4 (next): add back a single bare COUNTRY line, still with
zero ARENA lines, to further isolate whether COUNTRY itself is fine and
the problem is specifically in ARENA lines.

--- Fifth test session: experiment #4 (bare COUNTRY line) result ---
Sent: COUNTRY International\r\nENDCOUNTRYLIST\r\n (zero ARENA lines)
Client reaction: "an error has occured." again (same as experiment #2).

Combined with experiment #3 (empty list works cleanly), this narrows
the fault precisely to the COUNTRY line's own format - not anything
ARENA-specific, since ARENA lines weren't even present this round.

Experiment #5 (next): add a trailing arena-count field to the COUNTRY
line, guessing it needs one the way ARENA lines carry numeric fields:
    COUNTRY International <count>\r\n

--- Sixth test session: experiment #5 CONFIRMED WORKING + new RETRARENALIST ---
Sent: COUNTRY International 1\r\nENDCOUNTRYLIST\r\n (trailing arena-count
field added to the COUNTRY line)

Client reaction: SUCCESS. "Select Arena" tree populated cleanly with an
"International" node showing count 1, status text changed to "retrieving
arena list" (progressing normally, no error). This is the first fully
CONFIRMED (runtime) response format in this project.

The client then automatically sent a brand-new command, never seen or
known about before at all (not even as a compiled string constant we'd
noticed):

  > RETRARENALIST International\r\n   (bare, no CLAUTH envelope)

...then, ~38 seconds later, having received no response yet:

  > BOGUS\r\n

BOGUS's purpose is UNKNOWN - possibly a liveness/keepalive probe after
an idle timeout waiting for RETRARENALIST's response, possibly something
else entirely. No handler implemented for it yet.

Added commands/retrarenalist.py with a guessed response reusing the
already-confirmed ARENA and ENDARENALIST tokens:
  ARENA <name> <player_count> <max_players>\r\n   (per arena)
  ENDARENALIST\r\n
Verified locally end-to-end (MCC -> METAARENALIST -> RETRCOUNTRIES ->
RETRARENALIST, replaying the exact real-observed sequence). Awaiting the
next real client reaction.

--- Seventh test session: RETRARENALIST experiment #1 result ---
Sent: ARENA Test 0 20\r\nENDARENALIST\r\n
Client reaction: "an error has occured." again (same generic error as
RETRCOUNTRIES's early rejected attempts). The "International" node with
count 1 kept displaying correctly throughout, confirming RETRCOUNTRIES
itself is unaffected - the fault is specifically in this new response.

Experiment #2 (next): bare ENDARENALIST\r\n alone (empty arena list, no
ARENA lines), applying the same isolation technique that found
RETRCOUNTRIES's fix, to check whether the basic framing/terminator is
right before troubleshooting the ARENA line format itself.

--- Eighth test session: RETRARENALIST experiment #2 confirmed working ---
Sent: ENDARENALIST\r\n (empty arena list, no ARENA lines)
Client reaction: no error - status stayed at "retrieving arena list",
no crash. Confirms ENDARENALIST alone is a valid terminator here too,
mirroring RETRCOUNTRIES/ENDCOUNTRYLIST. Fault isolated to the ARENA
line's own format specifically.

Experiment #3 (next): a single minimal ARENA <name>\r\n line (no
players/maxplayers fields), mirroring the exact step-by-step approach
that found COUNTRY's fix.

--- Ninth test session: RETRARENALIST experiment #3 rejected ---
Sent: ARENA Test\r\nENDARENALIST\r\n (name only, zero extra fields)
Client reaction: "an error has occured." again.

This breaks the analogy with COUNTRY (name-alone failed, name+count
succeeded): here BOTH a bare name (exp #3) and name+2 numbers (exp #1)
fail. Missing trailing fields alone doesn't explain it the same way.

Experiment #4 (next): ARENA <name> <player_count> (single trailing
field, no max_players) - continuing the field-count ladder before
jumping to a bigger hypothesis (e.g. host/port info being required).

--- Tenth test session: RETRARENALIST experiment #4 rejected ---
Sent: ARENA Test 0\r\nENDARENALIST\r\n (single trailing field, player
count only, no max_players)
Client reaction: "an error has occured." again.

All three field-count variants now tried (name only, name+1, name+2)
fail. Not converging like COUNTRY's fix did - simply adding/removing
numeric fields doesn't seem to be the issue for ARENA.

Experiment #5 (next): pivot away from the field-count ladder to a
structurally different hypothesis - an arena entry is something the
client will try to actually connect to, unlike a country label, so
testing whether it needs host/port info:
    ARENA <name> <host_ip> <port>\r\n
(host_ip/port values are placeholders/internal addresses for now - this
tests whether the shape is accepted at all, not real connectivity.)

--- Eleventh test session: RETRARENALIST experiment #5 rejected ---
Sent: ARENA Test 100.64.0.2 7072\r\nENDARENALIST\r\n (name+host+port)
Client reaction: "an error has occured." again.

Four variants down (name-only, +1 field, +2 fields, +host+port) - none
work. No longer converging as a field-count problem.

Experiment #6 (next): new hypothesis - restate the country per ARENA
line, since RETRARENALIST's response is a flat list, not nested:
    ARENA <country> <name> <player_count> <max_players>\r\n
If this also fails, worth returning to static analysis rather than
continuing to guess field combinations blindly.

--- Twelfth test session: RETRARENALIST experiment #6 rejected ---
Sent: ARENA International Test 0 20\r\nENDARENALIST\r\n
Client reaction: "an error has occured." again. Six straight rejections.

Went back to static analysis rather than keep guessing field
combinations. A wider byte-range dump around the METAARENALIST/ARENA/
COUNTRY string-constant table (0x14c380-0x14c8f0 in gitr2k_unpacked.exe)
revealed two previously-missed tokens:
  "GETARENA " (9 bytes, trailing space baked into the constant - same
               pattern already confirmed for "METAMSG ")
  " "         (standalone single-space constant, likely a deduplicated
               field-separator literal)
"ARENA" (5 bytes, no space) appears twice in this table but every
experiment so far only ever tried it as the line prefix - GETARENA has
never been tested.

Experiment #7 (next): GETARENA <name> <player_count> <max_players>\r\n
as the line prefix instead of ARENA, keeping ENDARENALIST (confirmed
working, experiment #2) as the terminator.

--- Thirteenth test session: GETARENA doesn't error, but doesn't resolve either ---
Sent: GETARENA Test 0 20\r\nENDARENALIST\r\n
Client reaction: no "an error has occured." this time - but "Test" never
appeared under "International" either, and status stayed at "retrieving
arena list" indefinitely. Waited and confirmed BOGUS\r\n repeats
periodically (09:36:19, then again 09:36:59 - a ~40s interval), proving
it's a keepalive/liveness probe, not a one-shot "giving up" signal.

This means GETARENA, like ARENA, hasn't actually solved RETRARENALIST -
it's merely not rejected as malformed. It also means experiment #2
(bare ENDARENALIST, empty list) was only ever confirmed "doesn't error
immediately" - we never checked whether BOGUS would also appear there,
so it may not be a true confirmed-working terminator either.

Added commands/bogus.py: experiment #1 for BOGUS itself - echo it back
verbatim, to test whether acknowledging the keepalive unblocks whatever
the client is actually waiting on.

--- Fourteenth test session: BOGUS echo confirmed to have no effect ---
Watched the same connection for 4+ minutes after sending
GETARENA Test 0 20\r\nENDARENALIST\r\n. BOGUS\r\n repeated 6 times,
exactly 40s apart (10:58:56, 10:59:36, 11:00:16, 11:00:56, 11:01:36,
11:02:16), and our server echoed it back verbatim each time. No change
in client behavior whatsoever - arena list never populated, no new
message types appeared, BOGUS kept repeating on the same fixed interval
regardless of our response.

CONCLUSION: BOGUS is unrelated network-level keepalive noise (probably
to prevent an idle-connection timeout somewhere in the path), not a
protocol-semantic message the server needs to acknowledge. Echoing it
back has no bearing on whether RETRARENALIST's response is accepted.

RETRARENALIST's real response format remains unconfirmed after 7
content attempts (6 ARENA-prefixed shapes that error, 1 GETARENA-prefixed
shape that doesn't error but doesn't resolve either).

--- Shifting focus to MCC (arena.exe registration ack) per operator direction ---
Paused RETRARENALIST after 7 content attempts + BOGUS ruled out (see
above). Went back to static analysis of arena.exe specifically, since
docs/protocol.md section 4 already had an unanalyzed lead:
GMCC.OnArenaOwner handler (GMCCArenaOwner, 0x0049F8F0) - "location only,
body not yet analyzed" per the original disassembly pass.

Disassembling it revealed it's a tiny event-relay stub, not directly
informative. But investigating the surrounding string-constant table
found two new tokens: "GRANTED" (7 bytes) and "OWNER" (5 bytes),
clustered together. Exhaustive reference search: GRANTED has ZERO
findable references anywhere in the binary; OWNER's trivial getter
function has exactly one reference (itself, meaning it's an active
"return this literal" getter, likely a command-name property).

Experiment #1 for MCC: respond with "OWNER <arena name>\r\n" (e.g.
"OWNER Test\r\n"), echoing the registered arena's name back with OWNER
as the acknowledgment token. Verified locally. Awaiting real client
reaction - does arena.exe's Start button enable, or its activity log
show anything?

--- Fifteenth test session: MCC/OWNER CONFIRMED WORKING - three new commands unlocked ---
Real client test of the "OWNER <arena name>\r\n" experiment above.
Server log showed:

    CLAUTH Test 1234 MCC 0\r\n        (arena.exe -> server)
    OWNER Test\r\n                    (server -> arena.exe)
    SETPORT 7072\r\n                  (arena.exe -> server, bare)
    SETINFO 0 15\r\n                  (arena.exe -> server, bare)
    REQMOVES\r\n                      (arena.exe -> server, bare)

arena.exe's activity log, previously empty across every test, showed
"connected" followed by "retrieving moves database" - the first real
positive UI reaction from arena.exe in this entire investigation.

CONCLUSION: OWNER <arena name>\r\n is CONFIRMED as the MCC acknowledgment
(or at least sufficient to unblock arena.exe's registration flow) - the
first attempt worked, so GRANTED was never needed. This unblocks three
brand-new commands, all sent bare (no CLAUTH envelope, no arena name
repeated) on the same connection:

- SETPORT 7072        - arena.exe self-reporting its real listening port
                         (matches the CONFIRMED (disassembly) default of
                         7072 - this arena.exe instance wasn't reconfigured)
- SETINFO 0 15         - arena.exe self-reporting live stats; assumed
                         (player_count, max_players) by convention, not
                         independently confirmed (both values consistent
                         with either order for an empty, fresh arena)
- REQMOVES             - arena.exe requesting a "moves database"; matches
                         the "retrieving moves database" status text.
                         Response format completely unknown.

Implementation: added commands/setport.py and commands/setinfo.py, which
parse and record these into ArenaRegistry (server/registry.py: new
set_port()/set_info() methods), and commands/reqmoves.py, which
deliberately only observes/logs REQMOVES without guessing a response yet
- consistent with this project's isolate-one-variable methodology (the
same approach that eventually cracked RETRCOUNTRIES). Because these three
commands are bare and don't repeat the arena name, commands/mcc.py now
stashes the registering arena's name into the shared per-connection
context dict (context["arena_name"]) so the later handlers on the same
connection can find the right registry entry.

Verified locally by replaying the exact captured sequence
(CLAUTH Test 1234 MCC 0 -> SETPORT 7072 -> SETINFO 0 15 -> REQMOVES)
through the dispatcher: registry ends up with arena_port=7072,
player_count=0, max_players=15, and context["arena_name"] == "Test",
matching the real capture.

Open question for the next real test: does arena.exe's "Start" button
ever become clickable without a REQMOVES response? Does REQMOVES retry
periodically like BOGUS did if left unanswered? RETRARENALIST remains
paused - not touched in this session.

--- Sixteenth test session: re-confirms MCC/OWNER, RETRARENALIST still stuck - but reveals a framing bug ---
Fresh session against the just-deployed SETPORT/SETINFO/REQMOVES handlers.
Server log:

    CLAUTH Test 1234 MCC 0\r\n          (arena.exe -> server)
    OWNER Test\r\n                      (server -> arena.exe)
    SETPORT 7072\r\n                    (arena.exe -> server, own recv())
    SETINFO 0 15\r\nREQMOVES\r\n        (arena.exe -> server, ONE recv() -
                                          two commands coalesced together)

arena.exe's activity log again showed "connected" / "retrieving moves
database" (re-confirms the MCC/OWNER breakthrough). SETPORT was parsed
and recorded correctly (registry: arena_port=7072).

BUG FOUND: no COMMAND_SEEN log line ever appeared for SETINFO or
REQMOVES, even though the raw bytes for both were logged in the RECV
packet dump. Root cause: server/connection.py passed each raw recv()
chunk straight to dispatcher.dispatch() as a single blob, and the
dispatcher only looks at the first token in that blob. Since SETINFO and
REQMOVES arrived in the same TCP segment, only SETINFO's handler ever
fired (and only "worked" by accident, because Python's str.split() with
no separator also splits on \r\n, so the "REQMOVES" text got silently
absorbed as extra ignored tokens in setinfo.py's field parsing) -
REQMOVES itself was never dispatched, matched, or logged at all.

FIX: server/connection.py now buffers incoming bytes across recv() calls
and splits on \n before dispatching, so every \r\n-terminated command
gets its own individual dispatch() call no matter how the client batches
multiple commands into one TCP write. Verified locally by replaying the
exact captured byte sequence (including the coalesced SETINFO+REQMOVES
chunk) through the fixed connection-handling logic: all four commands
(MCC, SETPORT, SETINFO, REQMOVES) now dispatch individually and
correctly, with REQMOVES finally reaching commands/reqmoves.py.

Meanwhile in the gitr2k.exe "Select Arena" dialog (separate connection,
same test session): METAARENALIST -> RETRCOUNTRIES populated the
"International" node (Users=1), consistent with prior sessions.
RETRARENALIST experiment #7 (GETARENA) was sent again and behaved
exactly as before: no error, but "retrieving arena list" never resolves.
No new information here - RETRARENALIST remains paused, untouched this
session.

Because of the framing bug above, we have never actually seen a real,
correctly-logged REQMOVES COMMAND_SEEN event yet - only inferred its
presence from the raw byte dump and arena.exe's UI reaction. The next
real test, with the framing fix in place, should be the first one to
actually observe it.

--- Seventeenth session: framing fix verified live, REQMOVES finally captured correctly ---
Real test against the deployed framing fix. Log now shows REQMOVES
correctly split out and logged as its own COMMAND_SEEN event, distinct
from SETINFO, even though both were still sent in the same recv() as
before:

    CLAUTH Test 1234 MCC 0\r\n
    OWNER Test\r\n
    SETPORT 7072\r\n
    SETINFO 0 15\r\nREQMOVES\r\n   (one recv(), now split into TWO dispatches)
    -> COMMAND_SEEN ... SETINFO ...
    -> COMMAND_SEEN ... REQMOVES ...

This is the first time this project has actually observed a real,
correctly-logged REQMOVES event (previously it was silently dropped -
see prior entry). Confirms server/connection.py's line-buffering fix
works against real traffic, not just the local replay test.

arena.exe's UI still showed "connected" / "retrieving moves database"
(highlighted, i.e. current/pending status), "Start" still greyed out -
consistent with REQMOVES getting no response in this particular test
(the fix was deployed but REQMOVES still returned None at the time this
capture was taken - the MOVESDB experiment below was added immediately
after analyzing this log).

Went back to static analysis specifically for REQMOVES/MOVESDB, since
this project had PREVIOUSLY, INCORRECTLY dismissed MOVESDB as an
unrelated Pascal-scripting-engine keyword (grouped with BEGIN/WHILE/
FUNC/SCRIPT/PLUGIN/ACTIONSDB/SWEARDB). Found a real class
"TMovesDBCommand" in arena.exe's string-constant table, directly
clustered with "REQMOVES" and an "invalid response" (16 bytes) error
string - this is a genuine protocol command class, the earlier dismissal
was wrong. A near-identical sibling class references "REQSWLIST"/
"SWEARDB" with the same code shape (likely a swear-word/chat-filter
list), confirming a small family of "request a named database" commands.

Exhaustive raw-bytes cross-reference search (searching for the compiled
string constants' own virtual addresses as literal 4-byte little-endian
values elsewhere in the binary) found exactly one call site each for
REQMOVES and MOVESDB, both inside a single method at arena.exe VA
0x461d34-0x461e92. Disassembling it revealed:

  1. Sends "REQMOVES" via a virtual call (VMT+0x84) - matches confirmed
     wire traffic.
  2. Reads exactly ONE line via a ReadLn-shaped virtual call (VMT+0x74,
     terminator "\r\n") - no loop for further lines, unlike
     RETRCOUNTRIES/RETRARENALIST/METAARENALIST's multi-line responses.
  3. Takes Copy(line, 1, 7) and compares it against the literal
     "MOVESDB" (7 chars) via a CompareStr-shaped call. Mismatch ->
     raises the "invalid response" error found in the string table.
  4. On match, hands the WHOLE line to a further virtual method
     (VMT+0x9c, not disassembled) for parsing - so fields after
     "MOVESDB" remain unknown.

EXPERIMENT #1 for REQMOVES, now grounded in real disassembly rather than
guessed blind: respond with a bare

    MOVESDB\r\n

This satisfies the confirmed prefix check with nothing after it,
following the same start-minimal methodology that eventually cracked
RETRCOUNTRIES. Implemented in commands/reqmoves.py, verified locally by
replaying the full MCC->OWNER->SETPORT->SETINFO->REQMOVES sequence and
confirming the dispatcher now returns b'MOVESDB\r\n' for REQMOVES.

Awaiting real client reaction: does arena.exe accept this (does "Start"
enable, does the activity log move past "retrieving moves database")?
Does it show a visible/logged error? Does REQMOVES retry if rejected?
