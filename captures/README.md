# captures/

Empty until the server is run and connects with a real client. Each run
creates a paired `session_<timestamp>.log` (human-readable) and
`session_<timestamp>.jsonl` (machine-readable, one JSON record per line)
file here automatically — see `server/logger.py`.
